{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "s=\"$(git rev-parse --show-toplevel 2>/dev/null)/scripts/check-migrations-committed.sh\"; if [ -x \"$s\" ]; then bash \"$s\"; else exit 0; fi",
            "statusMessage": "Checking migrations are committed",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
